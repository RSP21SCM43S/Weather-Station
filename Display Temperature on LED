from sense_hat import SenseHat

sense = SenseHat();
sense.clear();

temp = sense.get_temperature();
s = str(temp)

r=0;
g=0;
b=252;

color = (252, 0, 252);

sense.show_message(s[:4],back_colour=[r,g,b]);
